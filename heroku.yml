build:
  docker:
    web: Dockerfile
run:
  web: npm start
  worker:
    command:
      - chmod +777 /usr/lib/python3/dist-packages/ --recursive && rasa run -m ~/rasa/models/ --enable-api --log-file ~/rasa/out.log -p 5005 --cors "*"
    image: web      
